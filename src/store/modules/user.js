// import * as mutTypes from '../mutation-types'
// import * as actTypes from '../action-types.js'

const state = {
  // showLoginGuideDialog: false
}

const getters = {}

const actions = {
  // [actTypes.FETCH_USER_INFO] ({ commit, state }) {}
}

const mutations = {
  // [mutTypes.TOGGLE_LOGIN_DIALOG] (state, showLoginDialog) {
  //   state.showLoginDialog = showLoginDialog
  // }
}

export default {
  state,
  getters,
  actions,
  mutations
}
